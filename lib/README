Directory /lib should be used for those base objects that are usually
used as inheritable base objects, but can also be used as stand-alone
cloneable objects. Those objects that are always cloned, and usually
don't even need any customizing should be placed under /std/obj.

Doomdark, 19-aug-98
