#include <npc.h>

void
create_monster(){
	set_name("gnome");
	set_id(({"gnome","miniature gnome","miniaturized gnome","perfectly miniaturized gnome"}));
      set_short("A perfectly miniaturized gnome");
      set_level(1);
      set_money(30);
      set_wc(5);
      set_ac(2);
      set_race("gnome");
  }

