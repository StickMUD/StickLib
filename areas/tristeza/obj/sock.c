/* Coded by MacLeod */

#include <smoke.h>

void create_smoke() {
    set_brand("Slugger's used socks");
    set_id(({"sock","used socks","socks"}));
    set_long(
      "The stench is intolerable. Slugger has worn these for 2 years in a row.\n\
Remember: Smoking endangers your health! /Healers"
    );
    set_strength(8);
    set_fuel(10);
}
