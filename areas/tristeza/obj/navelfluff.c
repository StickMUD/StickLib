/* some navel fluff - an important spell component... */
/* Bum has one piece of this */

#include <treasure.h>

void
create_treasure()
{
    set_name("navel fluff");
    set_id(({ "some navel fluff", "fluff" }));
    set_weight(0);
    set_short("Some navel fluff");
    set_long("How disgusting!");
    set_value(0);
}
