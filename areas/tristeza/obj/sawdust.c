/* A pinch of sawdust - an important spell component... */

#include <treasure.h>

void
create_treasure()
{
    set_name("pinch of sawdust");
    set_id(({ "sawdust", "pinch" }));
    set_weight(0);
    set_short("a pinch of sawdust");
    set_long("It's just a pinch of sawdust.");
    set_value(0);
}
