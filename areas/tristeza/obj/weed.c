/* Coded by MacLeod */

#include <smoke.h>

void create_smoke() {
    set_brand("Altairian Sentient Hop Weed");
    set_id(({"weed","hop weed"}));
    set_long("It's best if you don't look too closely at this pouch.\n\
Remember: Smoking endangers your health! /Healers"
    );
    set_strength(6);
    set_fuel(10);
}
