#include <cloth.h>

void
create_cloth()
{
  set_name("suomi-verkkarit");
  set_short("Suomi-verkkarit");
  set_id(({ "cloth", "clothing", "verkkarit" }));
  set_cloth_type("dress");
  set_long(
"'Suomi-verkkarit' is a sports clothing which were used by finnish \
athletes. They are white and blue, and have big white letters 'SUOMI' \
in the front.");
  set_weight(1);
  set_value(5000);
}
