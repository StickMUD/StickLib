#include <food.h>

void
create_food()
{
    set_name("bar of Suffeli");
    set_id(({"bar","chocolate", "food", "suffeli"}));
    set_short("A bar of Suffeli chocolate");
    set_long("It is a bar of Finnish Suffeli chocolate.");
    set_value(1);
    set_weight(1);
    set_strength(1);
    set_max_full(1);
    set_full(1);
    set_eater_mess("You devour the bar of Suffeli. Sweet!");
    set_eating_mess(" eats a bar of Suffeli chocolate.");
}
