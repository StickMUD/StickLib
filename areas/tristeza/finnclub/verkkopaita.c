#include <cloth.h>

void
create_cloth()
{
  set_name("verkkopaita");
  set_short("Verkkopaita");
  set_id(({ "cloth", "clothing", "paita", "shirt", "verkkopaita" }));
  set_cloth_type("shirt");
  set_long(
"It is a traditional finnish sleeveless T-shirt made of white cotton \
thread woven into a \"net\" (mesh diameter about 5 mm). It is \
a warm underwear.");
  set_weight(1);
  set_value(600);
}
