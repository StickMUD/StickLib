#include <armour.h>

void
create_armour()
{
	set_name("round shield");
   set_type("shield");
  set_ac(1);
 set_short("A round shield");
 set_long("It's just an ordinary metal shield.");
 set_weight(2);
 set_value(200);
}
