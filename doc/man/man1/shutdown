Command: shutdown <minutes | 'abort' | 'NOW'> [reason]
 
Used to control the reboot of the game. You can set a delayed shutdown,
or abort a delayed shutdown already in progress. An immediate shutdown
has absolutely no warning to players or coders. A reason must be given
for doing an immediate shutdown.
 
Various security levels are required for performing a shutdown.
