NAME
	conv - universal converter :)

DESCRIPTION
	Convert units within and between different scales,
	e.g. C <-> F, kg <-> lb, cm <-> in, etc.
	Valid units include:
	(length) in, ft, yd, mi, mm, cm, m, km,
	(weight) oz, lb, g, kg,
	(temp) f, c.

USAGE
	conv <int_or_float> <from_unit> <to_unit>
