NAME
	dq

DESCRIPTION
	Drop internal command queue. System uses internal command queues
	to queue timed commands so that all the command will get executed
	in the same order as they were typed in by the user. user can drop
	this queue at will and timed queue is interrupted. Now the user can
	type new commands instantly.

SEE ALSO
	alias, command, quicktyper.

//Tron
